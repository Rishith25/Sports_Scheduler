<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link rel="stylesheet" href="./css/styles.css" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body style="background-color: #f2f2f2;">
    <div class="container mx-auto py-8">
      <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
          <div
            class="col-start-3 col-span-2 bg-white border-gray-300 border mr-4 rounded p-5"
          >
            <%- include('header') %>
            <a href="/" class="text-green-600">Home Page</a>
            <p class="py-2">
              Already have an account?
              <a href="/login" class="text-green-600">Sign-in</a>
            </p>
            <h6 class="py-4 text-lg font-semibold">Sign-up as a New User</h6>
            <div>
              <form action="/users" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div class="py-3">
                  <label for="firstName" class="text-gray-700"
                    >First Name:</label
                  >
                  <input
                    type="text"
                    name="firstName"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-green-500"
                  />
                </div>
                <div class="py-3">
                  <label for="lastName" class="text-gray-700">Last Name:</label>
                  <input
                    type="text"
                    name="lastName"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-green-500"
                  />
                </div>
                <div class="py-3">
                  <label for="email" class="text-gray-700">Email:</label>
                  <input
                    type="email"
                    name="email"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-green-500"
                  />
                </div>
                <div class="py-3">
                  <label for="password" class="text-gray-700">Password:</label>
                  <input
                    type="password"
                    name="password"
                    class="border border-gray-300 text-gray-900 rounded p-2 w-full focus:outline-none focus:ring-2 focus:ring-green-500"
                  />
                </div>
                <div class="py-3">
                  <button
                    type="submit"
                    class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2"
                  >
                    Sign up
                  </button>
                </div>
              </form>
              <% if(messages && messages.error && messages.error.length> 0) { %> <%
                for(var i=0; i < messages.error.length; i++) { %>
                <li>
                  <%= messages.error[i] %>
                  <br />
                </li>
                <% } %> <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
