<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/styles.css" />
    <title>Sports Scheduler</title>
  </head>

  <body class="bg-gray-200">
    <nav class="bg-gray-800">
      <div
        class="container mx-auto px-4 py-2 flex justify-between items-center fixed bg-gray-800"
      >
        <div class="text-white text-lg font-semibold">
          <a href="/home"><%= title %></a>
          <span class="text-gray-300 mr-4 px-2 py-1 rounded hover">
            <a href="/home">Home</a>
          </span>
          <span class="text-gray-300 mr-4 px-2 py-1 rounded hover">
            <a href="/user/changePassword">Change Password</a>
          </span>
        </div>

        <div>
          <span class="text-gray-300 mr-4">Welcome <%= userName %></span>
          <a
            href="/signout"
            class="inline-block px-4 py-2 text-white bg-red-600 rounded-md font-medium"
            >Sign Out</a
          >
        </div>
      </div>
    </nav>
    <br>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <div class="container mx-auto py-8">
          <div
            class="col-start-3 col-span-2 bg-white mr-4 rounded"
            style="padding: 20px"
          >
            <h4 class="text-2xl font-semibold text-gray p-4 font-serif">
              Create A New Sport
            </h4>
            <form action="/sports" method="post">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <div class="flex-gap-2 py-4 px-2 items-center">
                <div class="flex-auto">
                  <input
                    type="text"
                    name="sportsname"
                    placeholder="Enter Sport Name"
                    class="border border-gray-300 rounded-lg text-gray-900 w-full p-4 text-sm focus:outline-none focus:ring-2 focus:ring-red-500"
                  />
                </div>
                <br />
                <div class="flex-none">
                  <button
                    type="submit"
                    class="bg-green-600 text-white px-6 py-3 rounded-lg font-medium shadow-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500"
                  >
                    Submit
                  </button>
                </div>
              </div>
            </form>
            <% if(messages && messages.error && messages.error.length> 0) { %>
            <% for(var i=0; i < messages.error.length; i++) { %>
            <li>
              <%= messages.error[i] %>
              <br />
            </li>
            <% } %> <% } %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
